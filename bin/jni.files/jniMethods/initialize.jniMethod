void {CLASS_NAME}::initialize(JNIEnv *env)
{
    setClass(env);
    cacheConstructor(env);

{CLASS_SIGNATURES}
    // addNativeMethod("destroy", (void*)&{CLASS_NAME}::nativeDestroy, kTypeVoid, NULL);
    registerNativeMethods(env);
}